# Fallback to fbdev if modesetting/DRM fails
# This provides basic display without hardware acceleration

Section "Device"
    Identifier "Framebuffer"
    Driver "fbdev"
    Option "fbdev" "/dev/fb0"
EndSection

Section "Screen"
    Identifier "Default Screen"
    Device "Framebuffer"
EndSection
