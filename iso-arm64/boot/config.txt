# TuxOS ARM64 - Raspberry Pi 4 Boot Configuration

# Kernel and initramfs
kernel=kernel8.img
initramfs initrd.img followkernel

# 64-bit mode
arm_64bit=1

# Display settings
disable_overscan=1
hdmi_force_hotplug=1

# GPU memory (128MB for browser/graphics)
gpu_mem=128

# Enable DRM VC4 V3D driver
dtoverlay=vc4-kms-v3d

# USB and boot options
otg_mode=1

# Disable splash for faster boot (optional)
disable_splash=1

# Enable UART for debugging (optional)
enable_uart=1
